# Copy to .env. In dev, `auto` uses the same host as Metro (recommended — survives Wi‑Fi / hotspot changes).
# Or set an explicit URL: Metro exp://192.168.1.6:8082 → http://192.168.1.6:3000
# Android emulator with auto: usually http://10.0.2.2:3000
EXPO_PUBLIC_API_URL=auto
